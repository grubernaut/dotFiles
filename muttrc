set ssl_starttls=yes
set ssl_force_tls=yes
set imap_user = 'jake@hashicorp.com'
set imap_pass = "`security find-internet-password -g -a jake@hashicorp.com 2>&1| perl -e 'if (<STDIN> =~ m/password: "(.*)"$/ ) { print $1; }'`"
set from='jake@hashicorp.com'
set realname='Jake Champlin'
set folder = imaps://imap.gmail.com/
set spoolfile = imaps://imap.gmail.com/INBOX
set postponed="imaps://imap.gmail.com/[Gmail]/Drafts"
set header_cache = "~/.mutt/cache/headers"
set message_cachedir = "~/.mutt/cache/bodies"
set certificate_file = "~/.mutt/certificates"
set smtp_url = "smtp://jake@hashicorp.com@smtp.gmail.com:587"
set smtp_pass = "`security find-internet-password -g -a jake@hashicorp.com 2>&1| perl -e 'if (<STDIN> =~ m/password: "(.*)"$/ ) { print $1; }'`"
set move = no
set imap_keepalive = 300
auto_view text/html
alternative_order text/plain text/enriched text/html
set sort = threads
set sort_aux = reverse-last-date-received
set uncollapse_jump
set pager_stop
set include
set reverse_name
set envelope_from
set mail_check = 60
set imap_check_subscribed=yes
set editor="subl -w"
set recall=no
set smtp_authenticators = 'gssapi:login'
unset mark_old
unset imap_passive
unset move
set delete
unset confirmappend
set quit
set beep_new
set pipe_decode
set thorough_search
set mailcap_path = ~/.mutt/mailcap

# Compose
set sig_dashes
set edit_headers
set fast_reply
set fcc_attach
unset mime_forward
set forward_format = "Fwd: %s"       # format of subject when forwarding
set forward_decode                   # decode when forwarding
set attribution = "On %d, %n wrote:" # format of quoting header
set reply_to                         # reply to Reply to: field
set reverse_name                     # reply as whomever it was to
set include                          # include message in replies
set forward_quote                    # include message in forwards

# Query OSX Contacts
set query_command = "goobook query '%s'"
bind editor <Tab> complete-query
bind editor ^T    complete

source ~/.mutt/bindings.muttrc
source ~/.mutt/sidebar.muttrc
source ~/.mutt/colors.muttrc